<style>

  .rating{
    display: inline-block;

    position: relative;
    
    width: -webkit-fit-content;
    width: -moz-fit-content;
    width: fit-content;
    height: auto;

    white-space: nowrap;
  }

  .rating:before{
    content: attr(filter);
    position: absolute;
    
    display: block;
    
    color: dodgerblue;
    overflow: hidden;
    pointer-events: none;
    white-space: nowrap;
  }
</style>
<div class="rating" filter="<%= star_rating(star) %>">☆☆☆☆☆</div>
